# 笔试提交说明 —— Mini Atoms

> 候选人：罗永彪 · 岗位：AI Native 全栈工程师

## 一、在线访问链接

- **Demo 入口**：http://112.124.4.87:8082/
- **示例生成应用（番茄钟）**：http://112.124.4.87:8082/#/app/e656bb0f-7444-464b-8719-4c7ef8f82fad
- **GitHub 源码**：https://github.com/lyb19850811/atoms-demo

## 二、项目概述

**Mini Atoms** 是一个具备 Atoms 交互体验的「AI 智能体驱动应用生成」平台。用户用一句话描述需求，智能体（DeepSeek）生成一个完整、可交互的单文件网页应用，并在沙箱环境中实时可视化预览；支持对话式迭代修改、数据持久化与一键分享。

核心闭环：`输入想法 → 智能体生成应用 → 沙箱可视化预览 → 对话迭代 → 保存/分享`

## 三、实现思路与关键取舍

1. **生成「单文件 HTML」而非 React 工程**：这是最重要的取舍。若让 AI 生成多文件 React 项目，服务端需 `npm install` + 构建，复杂度与失败率陡增，6-8h 内难以保证稳定。单文件 HTML 天然可交互、可分享、可在 iframe 直接渲染，复杂度最低、闭环最稳。代价是生成物无法使用前端框架生态，但符合「快速可运行原型」的定位。
2. **沙箱隔离安全**：生成代码运行在 `<iframe sandbox="allow-scripts allow-forms allow-modals">`（不含 `allow-same-origin`），并与系统提示词双重约束（禁用 localStorage / 网络请求），确保生成物无法读取主应用数据或越权。
3. **零原生依赖的持久化**：选用 Node 内置 `node:sqlite`，本地（Node 24）与服务器（Node 22）行为一致，规避原生模块编译问题，部署零摩擦。
4. **LLM Provider 抽象层**：`server/llm.js` 将模型调用与业务解耦，后续切换 Claude/OpenAI 只需改一处；用 JSON 输出模式 + 正则兜底解析，保证返回结构稳定。
5. **单端口部署**：Express 同时托管前端产物与 API，无 CORS、无跨域配置，`pm2` 守护。
6. **轻量注册**：昵称即进入（演示流程，不做密码体系），把时间留给核心主流程——这是明确的取舍。

## 四、当前完成程度

**已完成 ✅**
- 完整生成闭环：自然语言 → 流式思考过程（真实 `reasoning_content`）→ 单文件应用 → 沙箱预览（桌面/移动端切换）
- 对话式迭代：基于上一版修改，保留未提及的原有功能；思考过程随对话历史持久化
- 数据持久化：用户 / 应用 / 对话历史（含思考过程）落库 SQLite，刷新不丢失
- 轻量注册/登录、用户信息展示、退出登录
- 独立发布：一键发布为独立 URL（`/p/:id`），像真实部署的产品页
- 代码预览：查看/复制生成应用的完整 HTML
- 语音输入：浏览器语音识别，说出需求即可生成
- 管理后台：用户/应用管理，带访问令牌鉴权；接口限流防护
- 自动化测试：后端 15 个（node:test）+ 前端 9 个（Vitest + React Testing Library）
- 错误处理、空状态、加载态、暗色主题 UI
- 已部署 ECS（`pm2` 守护、开机自启），GitHub 源码公开

**未做 / 简化 ❌（及原因）**
- 真实密码账号体系：昵称即身份（轻量定位，非本次重点）
- 多步骤 Agent 流程（需求澄清/自检）：单次生成已满足核心诉求
- 前端测试覆盖有限（当前覆盖用户状态、SSE 解析、ChatPanel 组件）

## 五、如果继续投入，我会如何扩展（按优先级）

1. **多步骤 Agent 流程**：需求澄清 → 架构拆解 → 生成 → 自检 → 修复，更贴近真实 Atoms 的「团队协作」体验
2. **应用模板 / 画廊**：预设模板 + 社区分享，降低冷启动成本，形成内容飞轮
3. **真实账号 + 配额计费**：按用户隔离、Token 用量统计与计费
4. **生成应用部署为独立静态站点**：发布物托管到 CDN/对象存储，而非当前同源直接渲染

## 六、技术架构

```
React(Vite) SPA ──/api──> Express ──> node:sqlite (users/apps/messages)
                              └──> DeepSeek API (deepseek-chat)
```

- 前端：React 18 + Vite + HashRouter（任意 IP:端口环境分享链接可用）
- 后端：Node.js + Express，单端口托管前端 + API
- 存储：Node 内置 SQLite
- LLM：DeepSeek（provider 抽象层，可切换）

## 七、本地运行 / 部署

详见 `README.md`（含本地开发、生产构建、ECS 部署、API 说明与设计取舍）。
