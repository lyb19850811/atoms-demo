# 笔试提交说明 —— Mini Atoms

> 候选人：罗永彪 · 岗位：AI Native 全栈工程师

## 一、在线访问链接

- **Demo 入口**：http://112.124.4.87:8082/
- **示例生成应用（番茄钟）**：http://112.124.4.87:8082/#/app/e656bb0f-7444-464b-8719-4c7ef8f82fad
- **GitHub 源码**：https://github.com/lyb19850811/atoms-demo

## 二、项目概述

**Mini Atoms** 是一个具备 Atoms 交互体验的「AI 智能体驱动应用生成」平台。用户用一句话描述需求，智能体（DeepSeek）生成一个完整、可交互的网页应用，并实时可视化预览；支持「先规划 → 确认 → 执行」的 Agent 流程、多智能体团队协作（Team Mode）、对话式迭代、数据持久化、一键发布与产物导出。

核心闭环：`输入想法 → 规划确认 → 智能体生成应用 → 可视化预览 → 对话迭代 → 保存/发布/导出`

## 三、实现思路与关键取舍

1. **生成「单文件 HTML」而非 React 工程（默认模式）**：这是最重要的取舍。若让 AI 生成多文件 React 项目，服务端需 `npm install` + 构建，复杂度与失败率陡增，6-8h 内难以保证稳定。单文件 HTML 天然可交互、可分享、可在 iframe 直接渲染，复杂度最低、闭环最稳。作为「扩展能力」，我另实现了**团队模式**：并行流水线 `产品经理 → 架构师∥设计师 → 工程师` 产出多文件工程，兼顾工程思维与稳定性。
2. **先规划后执行**：生成前先产出可编辑的「开发计划」并等待用户确认（可修改），符合真实 Atoms 的「需求澄清 → 确认 → 执行」交互，也避免模型一次跑偏浪费 Token。
3. **沙箱隔离安全**：生成代码运行在 `<iframe sandbox="allow-scripts allow-forms allow-modals">`（不含 `allow-same-origin`），并与系统提示词双重约束（禁用 localStorage / 网络请求），确保生成物无法读取主应用数据或越权。
4. **零原生依赖的持久化**：选用 Node 内置 `node:sqlite`，本地（Node 24）与服务器（Node 22）行为一致，规避原生模块编译问题，部署零摩擦。
5. **LLM Provider 抽象层 + 稳定性工程**：`server/llm.js` 将模型调用与业务解耦，后续切换 Claude/OpenAI 只需改一处；用 JSON 输出模式 + 正则兜底解析保证返回结构稳定，并加入「冗长推理看门狗」——当推理内容过长且正文为空时自动回退到非推理模型重试，避免卡死。
6. **单端口部署**：Express 同时托管前端产物与 API，无 CORS、无跨域配置，`pm2` 守护。
7. **轻量注册**：昵称即进入（演示流程，不做密码体系），把时间留给核心主流程——这是明确的取舍。

## 四、当前完成程度

**已完成 ✅**

- 完整生成闭环：自然语言 → 流式思考过程（真实 `reasoning_content`）→ 应用 → 可视化预览
- **先规划后执行**：生成前产出可编辑计划，确认（可修改）后才执行
- **团队模式**：`产品经理 → 架构师∥设计师 → 工程师` 并行多智能体流水线，产出多文件工程，每一步可见
- **三栏工作台**：`对话 | 预览 | 产物`；中间栏「预览/代码」两层 Tab（代码 Tab 内嵌文件标签页），产物栏文件树（20% 宽、可折叠）
- **对话式迭代**：基于上一版修改，保留未提及的原有功能；思考过程随对话历史持久化
- **数据持久化**：用户 / 应用 / 产物文件 / 步骤 / 对话历史（含思考过程）落库 SQLite，刷新不丢失
- 轻量注册/登录、用户信息展示、退出登录
- **独立发布**：一键发布为独立 URL（`/p/:id`，mode 感知入口文件），像真实部署的产品页
- **产物导出**：一键下载产物 ZIP（服务端零依赖打包）
- 代码预览：查看/高亮生成应用代码，多文件标签页切换
- 管理后台：用户/应用管理，带访问令牌鉴权；接口限流防护
- **停止生成**：生成中可一键中断
- **明亮/暗色主题**：默认明亮，可切换并持久化
- 自动化测试：后端 15 个（node:test）+ 前端 8 个（Vitest + React Testing Library）
- 错误处理、空状态、加载态
- 已部署 ECS（`pm2` 守护、开机自启），GitHub 源码公开

**未做 / 简化 ❌（及原因）**

- 真实密码账号体系：昵称即身份（轻量定位，非本次重点）
- 团队流水线自由编排 / function calling：当前为固定并行流水线 `pm → architect∥designer → engineer`（优先保证稳定闭环）
- 语音输入：曾实现，后因非 HTTPS 环境的 `secure context` 限制与网络约束回退（权衡后移除，保留代码思路）
- 前端测试覆盖有限（当前覆盖用户状态、SSE 解析、API 封装、ChatPanel 组件）

## 五、如果继续投入，我会如何扩展（按优先级）

1. **团队流水线自由编排 / function calling**：按任务类型动态编排多智能体、互相调用，更贴近真实 Atoms 的「团队协作」体验
2. **应用模板 / 画廊**：预设模板 + 社区分享，降低冷启动成本，形成内容飞轮
3. **真实账号 + 配额计费**：按用户隔离、Token 用量统计与计费
4. **生成应用部署为独立静态站点**：发布物托管到 CDN/对象存储，而非当前同源直接渲染
5. **产物在线编辑**：在产物文件树中直接编辑并回写，形成「生成 → 改 → 再生成」的完整闭环

## 六、技术架构

```
React(Vite) SPA ──/api（SSE）──> Express ──> node:sqlite (users/apps/files/steps/messages)
                                    └──> DeepSeek API (deepseek-v4-flash，回退 deepseek-chat)
```

- 前端：React 18 + Vite + HashRouter（任意 IP:端口环境分享链接可用）
- 后端：Node.js + Express，单端口托管前端 + API，SSE 流式返回
- 存储：Node 内置 SQLite（WAL 模式，含迁移）
- LLM：DeepSeek（provider 抽象层，可切换；推理模型 + 稳定回退模型）

## 七、本地运行 / 部署

详见 `README.md`（含本地开发、生产构建、ECS 部署、API 说明、环境变量与设计取舍）。
